# The impact of genetic background on eclosion timing across Drosophila timeless genotypes

\# Author: Cetline Sailer



This repository contains the data, analysis scripts, and computational workflows used for the Bachelor's thesis investigating the genetic basis of eclosion rhythm in *Drosophila melanogaster*.

The project focuses on the effect of the **tim01 mutation** and investigates whether its phenotypic effect on eclosion timing depends on the genomic background.


## Repository structure

```text
.
├──Rscripts/ 
│   ├── data    -> P0, F2_old, F2_new, Supplementary agarose gels
│   ├── output  -> figures, tables
│   ├── scripts
│   └── README.md
│
├── conda/
│   └── envs/
│
└── data/
    ├── ID_locus_allele_primers.txt
    └── bed/                   -> flank/del bed files of timeless gene
```



## Folder structure of Rscripts

```
scripts/
  00_setup.R                        - paths, constants, libraries (source first)
  utils_circ_stats.R                - circular stats + resampling helper functions
  detect_eclosion_template.R        - raw pixel intensity -> eclosion time (per recording)
  01_P0_positive_control.R          - Table 12, Figure 11, Table 13, Watson's test
  02_F2_old_genotype_comparison.R   - Table 14, Figure 16, Table 16, Table 17
  03_F2_new_genotype_comparison.R   - Table 18, Figure 20, Table 19, Table 19b (NEW)
  04_resampling_P0_vs_F2.R          - Figure 17 (both generations), Figure 19, Figure 21
  05_appendix_P0_without_air.R      - Appendix Figure 21 (P0 without air supply)
  06_appendix_P0_per_recording.R    - Appendix Figure 22 (P0 per recording)
  07_appendix_F2_old_pooled.R       - Appendix Table 21 + Figure 24, and builds
                                       all_recordings_combined.csv (needed by 02_)

Run 07_ before 02_ if you don't already have all_recordings_combined.csv -
it's the script that generates it from the six raw rec_060-065 files.

One comparison from your original Resampling_new.R (p0_JI / rec_031 vs.
F2_new mutant) is commented out at the bottom of 04_resampling_P0_vs_F2.R.
I couldn't find it referenced anywhere in results.docx, so I didn't fold it
into the main pipeline - check with Luisa/Jun whether it belongs before
uncommenting it.

data/
  P0/            all_res_new_corrected.csv, P0.csv
  F2_old/        genotypes_filtered.txt, all_recordings_combined.csv,
                 eclosion_genotypes_merged.csv (generated by 02_)
  F2_new/        genotypes_filtered_new.txt, merged.csv (generated by 03_),
                 eclosiontime_rec0<83..89>_results.csv,
                 manual_corrections/rec_0XX_corrections.csv

output/
  figures/       all PDFs land here
  tables/        all CSVs land here
```

## How to run

1. Copy your raw data files into the matching `data/` subfolders above
   (same filenames as your original scripts expected).
2. Open the project in RStudio (create a `.Rproj` in this folder if you
   don't have one yet: File → New Project → Existing Directory).
3. Run scripts in order: `00_setup.R` is sourced automatically at the top
   of every other script, so you can just run `01_`, `02_`, `03_`, `04_` in
   sequence. `detect_eclosion_template.R` is only needed if you're
   reprocessing raw recordings from scratch (change `rec_id` at the top to
   switch recordings).

## Note on `watson.two.test()` p-values

R's `circular::watson.two.test()` doesn't return an exact numeric p-value —
it reports which critical-value band the statistic falls into (e.g.
`0.001 < P < 0.01`). `print()` the test object to see the band and copy it
into your results table by hand, same as you already did for Table 17.

